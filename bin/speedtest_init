#!/usr/bin/env ruby

require 'fileutils'

dir = 'speedtest'
base = Dir.pwd
path = File.join(base, dir)

if Dir.exist?(path)
   warn "speedtest directory exists, not created"
else
   puts "[add] `#{path}'"
   FileUtils.mkdir_p(path)
end
