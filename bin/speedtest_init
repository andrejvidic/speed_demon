#!/usr/bin/env ruby

require_relative '../lib/speed_test'

cli = SpeedTest::CLI.parse(ARGV)

if cli.setup_options
  SpeedTest::Setup.execute(cli)
else
  warn "Speedtest CLI received no options!"
  system('speedtest_init -h')
end
